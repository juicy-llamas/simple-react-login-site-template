
const config = require( '../config' );
const jwt = require( 'jsonwebtoken' );

const auth_filter = ( req, res, next ) => {
    // get the token 
    // verify the token
    // jwt.verify( 
    // on success call next
    // on failure redirect the user to login
};
